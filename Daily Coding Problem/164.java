class Main {
    public int findDuplicate(int[] arr) {
        int len = arr.length;
        int sum = 0;
        for (int i : arr) {
            sum += i;
        }
        return sum - (1 + n) * n / 2;
    }
}